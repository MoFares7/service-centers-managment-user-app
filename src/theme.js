// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
        fontFamily: 'Cairo, sans-serif', // Set the font family to Cairo
    },
});

export default theme;